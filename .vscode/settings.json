// Workspace settings
{
    // Hides the files excluded in gitignore in vscode
    "files.exclude": {
        "*/*.aux": true,
        "*/*.fdb_latexmk": true,
        "*/*.fls": true,
        "*/*.log": true,
        "*/*.synctex.gz": true
    }
}